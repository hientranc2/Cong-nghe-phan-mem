# FCO Mobile

Prototype giao diện mobile (React Native + Expo) cho trang chủ FCO. Dùng để dựng nhanh giao diện tương đương phiên bản web hiện tại.

## Cài đặt

```bash
cd mobile
npm install
```

## Chạy thử

```bash
npm start
```

Sau khi chạy, chọn nền tảng (web/Android/iOS) từ menu của Expo.
